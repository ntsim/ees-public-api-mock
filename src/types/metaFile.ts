export interface MetaFileRow {
  col_name: string;
  col_type: string;
  label: string;
  indicator_grouping: string;
  indicator_unit: string;
  indicator_dp: string;
  filter_hint: string;
  filter_grouping_column: string;
}
